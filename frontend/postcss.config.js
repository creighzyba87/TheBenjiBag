// PostCSS configuration for TheBenjiBag frontend.
//
// This configuration lists only the Tailwind CSS and Autoprefixer
// plugins, which are required for Tailwind to function correctly
// and to automatically add vendor prefixes to CSS rules.

module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};