import { jwtDecode } from 'jwt-decode'; export function saveToken(t){localStorage.setItem('authToken',t)} export function getToken(){return localStorage.getItem('authToken')} export function clearToken(){localStorage.removeItem('authToken')} export function parseToken(){const t=getToken(); if(!t) return null; try{return jwtDecode(t)}catch{return null}} export function authHeader(){const t=getToken(); return t?{Authorization:Bearer }:{}}